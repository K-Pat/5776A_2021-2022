#include "main.h"

using namespace okapi;


extern Motor fourBarLift;
extern Motor fourBarLift2;

extern void updateFourBarLiftMacro();

extern double fourBarPID(double setpoint);

extern void autonFourBarPID(double setpoint);
